# ══════════════════════════════════════════════════════════
# Personal AI Memory System — Python Dependencies
# 安装：pip install -r requirements.txt
# 可选部分：按需安装对应功能的依赖
# ══════════════════════════════════════════════════════════

# ── 核心 Web 框架（必装）──────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# ── LLM（至少安装一个）──────────────────────────────────
openai>=1.12.0          # OpenAI / Azure / Ollama / 任意兼容服务
# anthropic>=0.18.0    # Anthropic Claude（可选）

# ── 向量存储（必装）─────────────────────────────────────
faiss-cpu>=1.7.4        # CPU 版；GPU 用 faiss-gpu

# ── 加密（必装）─────────────────────────────────────────
cryptography>=42.0.0

# ── HTTP 客户端（必装）──────────────────────────────────
requests>=2.31.0
httpx>=0.27.0

# ── 工具（必装）─────────────────────────────────────────
numpy>=1.26.0
schedule>=1.2.0

# ── 文本解析（推荐安装）─────────────────────────────────
trafilatura>=1.8.0      # 网页正文提取
beautifulsoup4>=4.12.0
lxml>=5.0.0
markdown>=3.5.0

# ── PDF 解析（可选）─────────────────────────────────────
pdfplumber>=0.10.0
# PyMuPDF>=1.23.0       # 备选 PDF 解析器

# ── 二维码生成（已改为前端生成，Python 端不再需要）────────
# qrcode[pil]>=7.4.2  # 前端使用 qrcode npm 包生成，无需安装
# Pillow>=10.0.0       # 仅 Whisper/Qwen2-VL 可选功能需要

# ── 语音识别 Whisper（视频/音频内容解析，可选）───────────
# openai-whisper>=20231117

# ── 视觉理解 Qwen2-VL（图片内容解析，可选，需大显存）────
# transformers>=4.37.0
# torch>=2.1.0
# accelerate>=0.26.0

# ── 视频下载（视频内容解析，可选）────────────────────────
# yt-dlp>=2024.1.0
# opencv-python>=4.9.0

# ── 语音合成 XTTS（语音播报，可选，体积较大）─────────────
# TTS>=0.22.0

# ── 测试──────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
